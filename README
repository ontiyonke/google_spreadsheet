spreadsheetReader is a python program to import data from a google  spread sheet.

Provide a one-way import of patient appointment information from a Google Documents Spreadsheet.

The enrollment worksheet uses list based feeds the first row is assumed to be the header row and 
every row after that, the first empty row terminates a data set.The month worksheet is cell based.
The data from each worksheet is converted into its original format and stored in a list that contains lists
of rows from the worksheet. 

